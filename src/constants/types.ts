export interface MenuType {
  id: number;
  name: string;
  active: boolean;
}

export interface GenereType {
  id: number;
  name: string;
}

export interface MovieType {
  id: number;
  title: string;
  poster_path: string;
  release_date: string;
  backdrop_path: string;
}
